#include <iostream>
#include <cmath>

int main()
{
    double x = 3.04;
    double y = 4;
    double z;

    // z = std::max(x, y); // compares variables who has highest
    // z = std::min(x, y); // compares variables who has lowest
    // z = pow(3, 3); // x raise to the power of y
    // z = sqrt(9); // finds square root
    // z = abs(-3); // returns absolute value, negative will be positive
    // z = round(x); // rounds off
    // z = ceil(x); // finds the ceiling or highest value of decimal
    // z = floor(x); // // finds the floor or lowest value of decimal

    std::cout << z;

    return 0;
}